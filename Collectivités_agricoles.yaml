components: 
  shemas: 
    Collectivite: 
    type: objet
    required:
        - id
        - nom
        - ville
        - specialiteAgricole
        - dateCreation
    properties:
        id:
          type: string
          format: uuid
          description: Identifiant unique de la collectivité
        nom:
          type: string
          description: Nom unique de la collectivité
        ville:
          type: string
          description: ville où exerce la collectivité
        specialiteAgricole:
          type: string
          description: Spécialité agricole exercée
        dateCreation:
          type: string
          format: date
          description: Date de création de la collectivité
        nombreMembres:
          type: integer
          description: Nombre total de membres inscrits
        presidentId:
          type: string
          format: uuid
          description: ID du président actuel (membre confirmé)

    Membre:
      type: object
      required:
        - id
        - nom
        - prenoms
        - dateNaissance
        - genre
        - adresse
        - telephone
        - dateAdhesion
      properties:
        id:
          type: string
          format: uuid
        nom:
          type: string
        prenoms:
          type: string
        dateNaissance:
          type: string
          format: date
        genre:
          type: string
          enum: [MASCULIN, FEMININ]
        adresse:
          type: string
        telephone:
          type: string
        email:
          type: string
          format: email
        metierExerce:
          type: string
        dateAdhesion:
          type: string
          format: date
        collectiviteId:
          type: string
          format: uuid
        role:
          type: string
          enum: [PRESIDENT, PRESIDENT_ADJOINT, TRESORIER, SECRETAIRE, MEMBRE_CONFIRME, MEMBRE_JUNIOR]
        ancienneteJours:
          type: integer
          description: Nombre de jours depuis l'adhésion

    Paiement:
      type: object
      required:
        - id
        - membreId
        - montant
        - datePaiement
        - typePaiement
      properties:
        id:
          type: string
          format: uuid
        membreId:
          type: string
          format: uuid
        collectiviteId:
          type: string
          format: uuid
        montant:
          type: number
          format: decimal
          description: Montant en Ariary (MGA)
        datePaiement:
          type: string
          format: date-time
        typePaiement:
          type: string
          enum: [ADHESION, COTISATION_MENSUELLE, COTISATION_ANNUELLE, PONCTUELLE]
        modePaiement:
          type: string
          enum: [MOBILE_MONEY, VIREMENT_BANCAIRE, ESPECES]
        reference:
          type: string
          description: Référence transaction
          
CompteTresorerie:
      type: object
      required:
        - id
        - collectiviteId
        - typeCompte
        - solde
      properties:
        id:
          type: string
          format: uuid
        collectiviteId:
          type: string
          format: uuid
        typeCompte:
          type: string
          enum: [CAISSE, BANQUE, MOBILE_MONEY]
        nomTitulaire:
          type: string
        detailsCompte:
          type: string
          description: "Pour banque : BRED-XXXXX / Pour mobile money : Numéro téléphone + Opérateur"
        solde:
          type: number
          format: decimal
          description: Solde actuel en MGA